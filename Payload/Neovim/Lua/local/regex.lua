-- this file contains the regex helper functions that map the
--   input string into a table telling the configuration how to
--   search for substrings. Because lua does not have a built
--   in regex library, we will be using neovims.

---! store the strings for the patterns to match !---

---! here we have the functions to call them !---
